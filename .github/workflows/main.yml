name: Java Maven CI with Node.js and Appium

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt'
        java-version: '11'

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Install npm to latest version
      run: |
        npm install -g npm

    - name: install Appium
      run: |
        npm install -g appium@next
    
    - name: Appium server version check
      run: |
        appium -v

    - name: install Appium android driver
      run: |
        appium driver install uiautomator2

    - name: Build with Maven
      run: mvn clean install

    - name: Run Appium tests
      run: |
        appium
        
    # - name: open simulator for android/iOS
    #   run: |

    # - name: run the command for testng.xml
    #   run: |
